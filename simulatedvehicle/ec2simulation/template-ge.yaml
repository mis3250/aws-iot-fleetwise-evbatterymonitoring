AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: AWS IoT FleetWise EV Simulation - Ubuntu 24.04 Final Production Revision

Parameters:
  Ec2KeyPair:
    Type: String
    Description: Name of SSH key pair (leave blank for none)
    Default: ""
  FleetSize:
    Type: String
    Default: "1"
    AllowedValues: ["1", "2"]

Conditions:
  IsFleetSize2: !Equals [!Ref FleetSize, "2"]
  KeyPairSpecified: !Not [!Equals [!Ref Ec2KeyPair, ""]]

Mappings:
  AMIRegionMap:
    us-east-1: {AMIID: ami-0bc0f64eea5d47edf}
    us-east-2: {AMIID: ami-0275817a3a9018e6c}
    us-west-2: {AMIID: ami-047a505f037be3a7d}
    eu-central-1: {AMIID: ami-043516556e409f874}
    eu-west-1: {AMIID: ami-0568015f8e029497e}

Resources:
  Ec2SecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Instance security group
  
  Ec2SecurityGroupSshIngress:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !GetAtt Ec2SecurityGroup.GroupId
      IpProtocol: tcp
      FromPort: 22
      ToPort: 22
      CidrIp: "0.0.0.0/0"

  # --- DATABASE LAYER ---
  TimestreamDatabase:
    Type: AWS::Timestream::Database
    Properties:
      DatabaseName: !Sub "FleetWiseDB-${AWS::StackName}"

  TimestreamTable:
    Type: AWS::Timestream::Table
    Properties:
      TableName: FleetWiseTable
      DatabaseName: !Ref TimestreamDatabase
      RetentionProperties:
        MemoryStoreRetentionPeriodInHours: "24"
        MagneticStoreRetentionPeriodInDays: "7"

  # --- PROVISIONING LAYER ---
  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement: [{Effect: Allow, Principal: {Service: lambda.amazonaws.com}, Action: "sts:AssumeRole"}]
      Policies:
        - PolicyName: IoTProvisioningPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: ["logs:*", "iot:*"]
                Resource: "*"

  CreateThingFunction:
    Type: AWS::Lambda::Function
    Properties:
      Handler: index.handler
      Runtime: python3.9
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 300
      Code:
        ZipFile: !Sub |
          import cfnresponse, boto3, json
          def handler(event, context):
              responseData = {}
              iot = boto3.client('iot', region_name='${AWS::Region}')
              try:
                  thing_name = event['ResourceProperties']['ThingName']
                  policy_name = f"{thing_name}-policy"
                  if event['RequestType'] == 'Create':
                      keys = iot.create_keys_and_certificate(setAsActive=True)
                      cert_arn = keys['certificateArn']
                      responseData.update({
                          'certificatePem': keys['certificatePem'],
                          'privateKey': keys['keyPair']['PrivateKey'],
                          'iotEndpoint': iot.describe_endpoint(endpointType='iot:Data-ATS')['endpointAddress']
                      })
                      policy = {"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["iot:*"], "Resource": ["*"]}]}
                      iot.create_policy(policyName=policy_name, policyDocument=json.dumps(policy))
                      iot.create_thing(thingName=thing_name)
                      iot.attach_policy(policyName=policy_name, target=cert_arn)
                      iot.attach_thing_principal(thingName=thing_name, principal=cert_arn)
                      cfnresponse.send(event, context, cfnresponse.SUCCESS, responseData)
                  elif event['RequestType'] == 'Delete':
                      try:
                          principals = iot.list_thing_principals(thingName=thing_name).get('principals', [])
                          for cert_arn in principals:
                              iot.detach_thing_principal(thingName=thing_name, principal=cert_arn)
                              iot.detach_policy(policyName=policy_name, target=cert_arn)
                              cert_id = cert_arn.split('/')[-1]
                              iot.update_certificate(certificateId=cert_id, newStatus='INACTIVE')
                              iot.delete_certificate(certificateId=cert_id, forceDelete=True)
                          iot.delete_thing(thingName=thing_name)
                          iot.delete_policy(policyName=policy_name)
                      except: pass
                      cfnresponse.send(event, context, cfnresponse.SUCCESS, responseData)
                  else:
                      cfnresponse.send(event, context, cfnresponse.SUCCESS, responseData)
              except Exception as e:
                  cfnresponse.send(event, context, cfnresponse.FAILED, {"Error": str(e)})

  IoTThing1:
    Type: Custom::IoTThing
    Properties:
      ServiceToken: !GetAtt CreateThingFunction.Arn
      ThingName: "blog-vehicle-01"

  IoTThing2:
    Type: Custom::IoTThing
    Condition: IsFleetSize2
    Properties:
      ServiceToken: !GetAtt CreateThingFunction.Arn
      ThingName: "blog-vehicle-02"

  # --- COMPUTE LAYER ---
  Ec2ServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement: [{Effect: Allow, Principal: {Service: ec2.amazonaws.com}, Action: "sts:AssumeRole"}]
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: FleetWiseInstancePolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action: ["s3:*", "timestream:*", "iot:*"]
                Resource: "*"
              - Effect: Allow
                Action: ["cloudformation:SignalResource"]
                Resource: "*"

  Ec2InstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles: [!Ref Ec2ServiceRole]

  Ec2Instance1:
    Type: AWS::EC2::Instance
    DependsOn: [IoTThing1, TimestreamTable]
    CreationPolicy:
      ResourceSignal: {Count: 1, Timeout: PT30M}
    Properties:
      ImageId: !FindInMap [AMIRegionMap, !Ref "AWS::Region", AMIID]
      InstanceType: c8g.xlarge
      IamInstanceProfile: !Ref Ec2InstanceProfile
      SecurityGroupIds: [!Ref Ec2SecurityGroup]
      KeyName: !If [KeyPairSpecified, !Ref Ec2KeyPair, !Ref "AWS::NoValue"]
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          # 1. Log Redirection
          exec > >(tee /var/log/user-data.log|logger -t user-data -s 2>/dev/console) 2>&1
          
          # 2. Wait for Apt Lock (Crucial for fresh boots)
          while fuser /var/lib/dpkg/lock-frontend >/dev/null 2>&1 ; do
            echo "Waiting for background software managers..."
            sleep 5
          done

          # 3. IMDSv2 Token & Instance ID
          TOKEN=$(curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")
          INSTANCE_ID=$(curl -H "X-aws-ec2-metadata-token: $TOKEN" -s http://169.254.169.254/latest/meta-data/instance-id)

          error_handler() {
            echo "Error occurred. Signaling failure..."
            /usr/local/bin/aws cloudformation signal-resource --stack-name ${AWS::StackName} --logical-resource-id Ec2Instance1 --unique-id "$INSTANCE_ID" --status FAILURE --region ${AWS::Region} || true
          }
          trap error_handler ERR

          # 4. Global Env & Ubuntu 24.04 Fixes
          export DEBIAN_FRONTEND=noninteractive
          # Suppress restart prompts
          if [ -f /etc/needrestart/needrestart.conf ]; then
            sed -i 's/#$nrconf{restart} = .*/$nrconf{restart} = "a";/' /etc/needrestart/needrestart.conf || true
          fi

          # 5. Core Dependencies
          apt-get update -y
          apt-get install -y build-essential python3-pip python3-numpy python3-pandas can-utils git jq unzip libssl-dev libcurl4-openssl-dev cmake clang-tidy clang-format libboost-all-dev zlib1g-dev libsnappy-dev
          
          # 6. AWS CLI
          curl "https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip" -o "awscliv2.zip"
          unzip -o awscliv2.zip && ./aws/install && rm -rf awscliv2.zip aws/

          # 7. Build FleetWise
          cd /home/ubuntu
          sudo -u ubuntu git clone https://github.com/aws/aws-iot-fleetwise-edge.git
          cd aws-iot-fleetwise-edge
          
          # Patch Clang Naming
          sed -i 's/clang-tidy-12/clang-tidy/g' tools/install-deps-native.sh
          sed -i 's/clang-format-12/clang-format/g' tools/install-deps-native.sh
          ln -s /usr/bin/clang-tidy /usr/bin/clang-tidy-12 || true
          ln -s /usr/bin/clang-format /usr/bin/clang-format-12 || true

          ./tools/install-socketcan.sh --bus-count 1 || true
          # Run deps without 'set -e' to ignore minor link warnings
          sudo ./tools/install-deps-native.sh || echo "Deps script finished with minor warnings."

          # NATIVE BUILD (Switch back to strict mode)
          set -e
          sudo -u ubuntu ./tools/build-fwe-native.sh

          # 8. Credentials & Configuration
          mkdir -p /etc/aws-iot-fleetwise
          echo "${IoTThing1.certificatePem}" > /etc/aws-iot-fleetwise/certificate.pem
          echo "${IoTThing1.privateKey}" > /etc/aws-iot-fleetwise/private.key
          curl -s https://www.amazontrust.com/repository/AmazonRootCA1.pem -o /etc/aws-iot-fleetwise/AmazonRootCA1.pem
          openssl rsa -in /etc/aws-iot-fleetwise/private.key -out /etc/aws-iot-fleetwise/private.key -traditional

          ./tools/configure-fwe.sh --input-config-file configuration/static-config.json \
            --output-config-file /etc/aws-iot-fleetwise/config-0.json \
            --vehicle-name blog-vehicle-01 --endpoint-url ${IoTThing1.iotEndpoint} \
            --certificate /etc/aws-iot-fleetwise/certificate.pem --private-key /etc/aws-iot-fleetwise/private.key \
            --root-ca /etc/aws-iot-fleetwise/AmazonRootCA1.pem
          
          sudo ./tools/install-fwe.sh /etc/aws-iot-fleetwise/config-0.json
          
          # Success Signal
          /usr/local/bin/aws cloudformation signal-resource --stack-name ${AWS::StackName} --logical-resource-id Ec2Instance1 --unique-id "$INSTANCE_ID" --status SUCCESS --region ${AWS::Region}

  Ec2Instance2:
    Type: AWS::EC2::Instance
    Condition: IsFleetSize2
    DependsOn: [IoTThing2, TimestreamTable]
    CreationPolicy:
      ResourceSignal: {Count: 1, Timeout: PT30M}
    Properties:
      ImageId: !FindInMap [AMIRegionMap, !Ref "AWS::Region", AMIID]
      InstanceType: c8g.xlarge
      IamInstanceProfile: !Ref Ec2InstanceProfile
      SecurityGroupIds: [!Ref Ec2SecurityGroup]
      KeyName: !If [KeyPairSpecified, !Ref Ec2KeyPair, !Ref "AWS::NoValue"]
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          exec > >(tee /var/log/user-data.log|logger -t user-data -s 2>/dev/console) 2>&1
          while fuser /var/lib/dpkg/lock-frontend >/dev/null 2>&1 ; do sleep 5; done
          TOKEN=$(curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")
          INSTANCE_ID=$(curl -H "X-aws-ec2-metadata-token: $TOKEN" -s http://169.254.169.254/latest/meta-data/instance-id)
          
          error_handler() {
            /usr/local/bin/aws cloudformation signal-resource --stack-name ${AWS::StackName} --logical-resource-id Ec2Instance2 --unique-id "$INSTANCE_ID" --status FAILURE --region ${AWS::Region} || true
          }
          trap error_handler ERR

          export DEBIAN_FRONTEND=noninteractive
          apt-get update -y
          apt-get install -y build-essential python3-pip python3-numpy python3-pandas can-utils git jq unzip libssl-dev libcurl4-openssl-dev cmake clang-tidy clang-format libboost-all-dev zlib1g-dev libsnappy-dev

          curl "https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip" -o "awscliv2.zip"
          unzip -o awscliv2.zip && ./aws/install && rm -rf awscliv2.zip aws/

          cd /home/ubuntu
          sudo -u ubuntu git clone https://github.com/aws/aws-iot-fleetwise-edge.git
          cd aws-iot-fleetwise-edge
          sed -i 's/clang-tidy-12/clang-tidy/g' tools/install-deps-native.sh
          sed -i 's/clang-format-12/clang-format/g' tools/install-deps-native.sh
          ln -s /usr/bin/clang-tidy /usr/bin/clang-tidy-12 || true
          ln -s /usr/bin/clang-format /usr/bin/clang-format-12 || true
          ./tools/install-socketcan.sh --bus-count 1 || true
          sudo ./tools/install-deps-native.sh || echo "Warnings ignored"

          set -e
          sudo -u ubuntu ./tools/build-fwe-native.sh

          mkdir -p /etc/aws-iot-fleetwise
          echo "${IoTThing2.certificatePem}" > /etc/aws-iot-fleetwise/certificate.pem
          echo "${IoTThing2.privateKey}" > /etc/aws-iot-fleetwise/private.key
          curl -s https://www.amazontrust.com/repository/AmazonRootCA1.pem -o /etc/aws-iot-fleetwise/AmazonRootCA1.pem
          openssl rsa -in /etc/aws-iot-fleetwise/private.key -out /etc/aws-iot-fleetwise/private.key -traditional

          ./tools/configure-fwe.sh --input-config-file configuration/static-config.json \
            --output-config-file /etc/aws-iot-fleetwise/config-0.json \
            --vehicle-name blog-vehicle-02 --endpoint-url ${IoTThing2.iotEndpoint} \
            --certificate /etc/aws-iot-fleetwise/certificate.pem --private-key /etc/aws-iot-fleetwise/private.key \
            --root-ca /etc/aws-iot-fleetwise/AmazonRootCA1.pem
          
          sudo ./tools/install-fwe.sh /etc/aws-iot-fleetwise/config-0.json
          /usr/local/bin/aws cloudformation signal-resource --stack-name ${AWS::StackName} --logical-resource-id Ec2Instance2 --unique-id "$INSTANCE_ID" --status SUCCESS --region ${AWS::Region}

Outputs:
  Ec2Instance1PublicIp:
    Value: !GetAtt Ec2Instance1.PublicIp
  IoTEndpoint:
    Value: !GetAtt IoTThing1.iotEndpoint
  TimestreamTable:
    Value: !Ref TimestreamTable